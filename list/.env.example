# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mailing_list
DB_USER=mailing_list_user
DB_PASS=change-me

# Shared mail credential encryption key for both services.
# Must match list-admin/.env once encrypted credentials are stored.
MAIL_CREDENTIALS_KEY=

# Mailer runtime
DEFAULT_IMAP_FOLDER=INBOX
STATE_DIR=state
LOG_FILE=logs/mlist.log

# Optional tuning
# MAX_SEEN_IDS=500
# DANGEROUS_EXT=.exe,.bat,.cmd,.scr,.js,.vbs,.ps1,.jar,.msi
# MAX_BYTES=10485760
# LOG_MAX_BYTES=5242880
# LOG_BACKUP_COUNT=7
# Increase to 90 or 120 in production if your IMAP provider occasionally responds slowly.
# SOCKET_TIMEOUT_SECONDS=60
# IMAP_BACKOFF_BASE_SECONDS=60
# IMAP_BACKOFF_CAP_SECONDS=1800
# BACKOFF_MINUTES=30
